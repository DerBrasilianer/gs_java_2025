# Configurações do Banco de Dados Oracle
spring.datasource.url=jdbc:oracle:thin:@oracle.fiap.com.br:1521:orcl
spring.datasource.username=RM559183
spring.datasource.password=fiap24
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# Dialeto do Hibernate para Oracle
spring.jpa.database-platform=org.hibernate.dialect.OracleDialect

# Não criar nem alterar tabelas automaticamente
spring.jpa.hibernate.ddl-auto=none

# Mostrar SQL no console (opcional, útil para debug)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Swagger/OpenAPI (se desejar configurar porta ou detalhes, opcional)
# server.port=8080